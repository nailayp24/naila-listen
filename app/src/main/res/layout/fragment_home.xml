<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/figma_orange">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimaryVariant"
        app:layout_constraintTop_toTopOf="parent"
        app:titleTextColor="@android:color/white" />

    <!-- Menggunakan ScrollView agar jika tombol banyak tetap bisa di-scroll -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="32dp"
            android:gravity="center">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="BookVibes"
                android:textSize="32sp"
                android:textStyle="bold"
                android:textColor="@color/white"
                android:layout_marginBottom="24dp"/>

            <!-- Tombol Pertemuan 2 -->
            <Button
                android:id="@+id/btnPertemuan2"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="Pertemuan 2 (Kalkulator)"
                android:textColor="@color/figma_orange"
                android:backgroundTint="@color/white"
                android:layout_marginBottom="16dp"/>

            <!-- Tombol Pertemuan 3 -->
            <Button
                android:id="@+id/btnPertemuan3"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="Pertemuan 3 (Login)"
                android:textColor="@color/figma_orange"
                android:backgroundTint="@color/white"
                android:layout_marginBottom="16dp"/>

            <!-- Tombol Pertemuan 4 -->
            <Button
                android:id="@+id/btnPertemuan4"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="Pertemuan 4 (Custom Layout)"
                android:textColor="@color/figma_orange"
                android:backgroundTint="@color/white"
                android:layout_marginBottom="16dp"/>

            <!-- Tombol Pertemuan 6 -->
            <Button
                android:id="@+id/btnPertemuan6"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="Web View Bina Desa"
                android:textColor="@color/figma_orange"
                android:backgroundTint="@color/white"
                android:layout_marginBottom="32dp"/>

            <Button
                android:id="@+id/btnMenuLogout"
                android:layout_width="200dp"
                android:layout_height="55dp"
                android:text="Logout"
                android:textColor="@color/white"
                android:backgroundTint="@android:color/transparent"/>

        </LinearLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>